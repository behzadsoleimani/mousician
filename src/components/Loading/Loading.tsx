import { LoadingContainer } from "./Loading.styles";

export const Loading = () => {
  return (
    <LoadingContainer>
      <span>.</span><span>.</span><span>.</span>
    </LoadingContainer>
  );
};
